name: Docker Image build

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:

  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v3
    - name: Build the Docker image
      run: docker build . --file static-website-boilerplate-master/Dockerfile --tag jankogaga/static-website-boilerplate-master:latest
    
    - name: Run the Docker image
      run: docker run -d jankogaga/static-website-boilerplate-master:latest
      
